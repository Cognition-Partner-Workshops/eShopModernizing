# Copy this file to `.env` and fill in a real value before running docker compose.
# Do NOT commit the resulting `.env` file (it is git-ignored).

# SQL Server 'sa' password. Must satisfy the SQL Server strong-password policy:
# at least 8 characters including uppercase, lowercase, digits and symbols.
MSSQL_SA_PASSWORD=ChangeMe_Strong!Passw0rd
