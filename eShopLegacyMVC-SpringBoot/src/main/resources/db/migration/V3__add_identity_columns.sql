-- Add GENERATED BY DEFAULT AS IDENTITY to CatalogType and CatalogBrand tables
-- to match @GeneratedValue(strategy = GenerationType.IDENTITY) on JPA entities

ALTER TABLE CatalogType ALTER COLUMN Id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 5);
ALTER TABLE CatalogBrand ALTER COLUMN Id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 6);
