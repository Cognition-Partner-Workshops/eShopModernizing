{
  "// Notes": "Sample configuration for the modernized catalog. Non-secret defaults only. The real ConnectionStrings:CatalogDb value is a SECRET and must be supplied at runtime via environment variables (e.g. ConnectionStrings__CatalogDb) or a secret store such as Azure Key Vault. Do NOT commit real connection strings.",

  "Catalog": {
    "UseMockData": false,
    "UseCustomizationData": false
  },

  "ConnectionStrings": {
    "// CatalogDb": "Placeholder only — override at runtime. Example (LocalDB): Server=(localdb)\\MSSQLLocalDB;Initial Catalog=Microsoft.eShopOnContainers.Services.CatalogDb;Integrated Security=True;MultipleActiveResultSets=True;TrustServerCertificate=True",
    "CatalogDb": ""
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  }
}
