@page
@model IndexModel
@{
    ViewData["Title"] = "Index";
}

<h2 class="esh-body-title">Catalog manager (Razor Pages)</h2>

<div class="esh-table">
    <form method="get" class="form-inline esh-filter">
        <label class="control-label">Brand</label>
        <select name="BrandFilter" asp-items="Model.Brands" class="form-control">
            <option value="">All</option>
        </select>

        <label class="control-label">Type</label>
        <select name="TypeFilter" asp-items="Model.Types" class="form-control">
            <option value="">All</option>
        </select>

        <input type="hidden" name="size" value="@Model.PageSize" />
        <button type="submit" class="btn esh-button esh-button-primary">Filter</button>
    </form>

    <p class="esh-link-wrapper">
        <a asp-page="Create" class="btn esh-button esh-button-primary">Create New</a>
    </p>

    @if (!Model.Model.Data.Any())
    {
        <table>
            <tr>
                <td>No data was returned.</td>
            </tr>
        </table>
    }
    else
    {
        <table class="table">
            <thead>
                <tr class="esh-table-header">
                    <th></th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Brand</th>
                    <th>Type</th>
                    <th>Price</th>
                    <th>Picture name</th>
                    <th>Stock</th>
                    <th>Restock</th>
                    <th>Max stock</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Model.Data)
                {
                    <tr>
                        <td><img class="esh-thumbnail" src="/Pics/@item.PictureFileName" /></td>
                        <td><p>@item.Name</p></td>
                        <td><p>@item.Description</p></td>
                        <td><p>@item.CatalogBrand?.Brand</p></td>
                        <td><p>@item.CatalogType?.Type</p></td>
                        <td><p><span class="esh-price">@item.Price</span></p></td>
                        <td><p>@item.PictureFileName</p></td>
                        <td><p>@item.AvailableStock</p></td>
                        <td><p>@item.RestockThreshold</p></td>
                        <td><p>@item.MaxStockThreshold</p></td>
                        <td>
                            <a asp-page="Edit" asp-route-id="@item.Id" class="esh-table-link">Edit</a>
                            |
                            <a asp-page="Details" asp-route-id="@item.Id" class="esh-table-link">Details</a>
                            |
                            <a asp-page="Delete" asp-route-id="@item.Id" class="esh-table-link">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>

<div class="esh-pager">
    <div class="container">
        <article class="esh-pager-wrapper row">
            <nav>
                @if (Model.Model.ActualPage > 0)
                {
                    <a class="esh-pager-item esh-pager-item--navigable"
                       asp-page="Index"
                       asp-route-index="@(Model.Model.ActualPage - 1)"
                       asp-route-size="@Model.Model.ItemsPerPage"
                       asp-route-brandfilter="@Model.BrandFilter"
                       asp-route-typefilter="@Model.TypeFilter">Previous</a>
                }

                <span class="esh-pager-item">
                    Showing @Model.Model.ItemsPerPage of @Model.Model.TotalItems products - Page @(Model.Model.ActualPage + 1) - @Model.Model.TotalPages
                </span>

                @if (Model.Model.ActualPage < Model.Model.TotalPages - 1)
                {
                    <a class="esh-pager-item esh-pager-item--navigable"
                       asp-page="Index"
                       asp-route-index="@(Model.Model.ActualPage + 1)"
                       asp-route-size="@Model.Model.ItemsPerPage"
                       asp-route-brandfilter="@Model.BrandFilter"
                       asp-route-typefilter="@Model.TypeFilter">Next</a>
                }
            </nav>
        </article>
    </div>
</div>
