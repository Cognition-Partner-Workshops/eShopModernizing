namespace eShop.Shared;

/// <summary>
/// Placeholder marker for the cross-cutting library (configuration/options, DI extensions,
/// logging, JSON serialization, health checks). Filled in by later modernization tickets.
/// </summary>
public static class SharedAssemblyMarker
{
}
